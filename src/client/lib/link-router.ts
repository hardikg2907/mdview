// STUB — replaced in Batch 7 Task 24
export function wireInternalLinks(
  _root: HTMLElement,
  _onNavigate: (relPath: string, hash: string) => void,
): void {
  // no-op until internal-link router is implemented
}
